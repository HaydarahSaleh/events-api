type: object
properties:
    success:
        type: boolean
        example: true
    statistics:
        type: object
        properties:
            visitors:
                type: number
                example: 10
            entities:
                type: number
                example: 10
            participants:
                type: number
                example: 10
    returnedTypeName:
        type: string
        example: statistics
