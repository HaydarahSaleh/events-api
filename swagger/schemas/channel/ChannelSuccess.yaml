properties:
    success:
        type: boolean
        example: true
    id:
        type: number
        example: 2
    title:
        type: string
        example: title

    file:
        $ref: "../file/File.yaml"

    createdAt:
        type: string
        example: 1-1-2020
    createdBy:
        $ref: "../user/User.yaml"
    publishMode:
        type: number
        example: 2
    order:
        type: number
        example: 2
    type:
        type: number
        example: phone
    value:
        type: number
        example: value
