type: object
properties:
    success:
        type: boolean
        example: true
    surveys:
        type: array
        items:
            $ref: "./Survey.yaml"

    limit:
        type: number
        example: 100
    offset:
        type: number
        example: 0
