type: object
properties:
    success:
        type: boolean
        example: true
    requests:
        type: array
        items:
            $ref: "./ServiceRequest.yaml"

    limit:
        type: number
        example: 100
    offset:
        type: number
        example: 0
