type: object
properties:
    success:
        type: boolean
        example: true
    themes:
      type: array
      items:
        $ref: "./Theme.yaml"   
    limit:
        type: number
        example: 100
    offset:
        type: number
        example: 0