type: object
properties:
    success:
        type: boolean
        example: true
    writers:
        type: array
        items:
            $ref: "./Trustee.yaml"

    limit:
        type: number
        example: 100
    offset:
        type: number
        example: 0
