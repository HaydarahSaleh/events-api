post:
    summary: Create
    description: <p>Creates a new <b>IsUsefull</b>. <p>Available for <b>All</b></p>

    tags:
        - career
    
    parameters:
        
        - $ref: "../../parameters/path/id.yaml"
    requestBody:
        $ref: "../../requests/rate/IsUsefull.yaml"
    responses:
        "201":
            $ref: "../../responses/rate/IsUsefull.yaml"

        "401":
            $ref: "../../responses/common/InvalidParameter.yaml"
        "402":
            $ref: "../../responses/common/NotFound.yaml"

        default:
            $ref: "../../responses/common/UnexpectedError.yaml"
