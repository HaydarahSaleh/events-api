post:
    summary: Create
    description: <p>Creates a new <b>FeedBack</b>. <p>Available for <b>All</b></p>

    tags:
        - link
    parameters:
        
        - $ref: "../../parameters/path/id.yaml"
    requestBody:
        $ref: "../../requests/rate/FeedBack.yaml"
    responses:
        "201":
            $ref: "../../responses/rate/FeedBack.yaml"

        "401":
            $ref: "../../responses/common/InvalidParameter.yaml"
        "402":
            $ref: "../../responses/common/NotFound.yaml"

        default:
            $ref: "../../responses/common/UnexpectedError.yaml"
