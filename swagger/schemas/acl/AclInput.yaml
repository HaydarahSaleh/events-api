type: object
required:
    - title
    - summary
    - startDate
    - photoId

properties:
    name:
        type: string
        example: name
    userGroups:
        type: array
        items:
            type: number
        example: [1, 2, 3]
