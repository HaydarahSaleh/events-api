type: object
properties:
    success:
        type: boolean
        example: true
    winners:
        type: array
        items:
            $ref: "./Winner.yaml"

    limit:
        type: number
        example: 100
    offset:
        type: number
        example: 0
