type: object
properties:
    success:
        type: boolean
        example: true
    channels:
        type: array
        items:
            $ref: "./Channel.yaml"

    limit:
        type: number
        example: 100
    offset:
        type: number
        example: 0
